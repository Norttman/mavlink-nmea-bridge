CC = gcc
CFLAGS = -Wall -O2 -I. -Wno-format-truncation
LDFLAGS = -lws2_32 -lm

TARGET = nmea_bridge.exe
SRC = src/main.c

all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) -o $(TARGET) $(SRC) $(LDFLAGS)

clean:
	rm -f $(TARGET)

.PHONY: all clean